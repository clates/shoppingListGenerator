import Layout from "../components/layout";
import NotImpl from "../components/NotImplemented";

export default function Page() {
  return (
    <NotImpl msg="Homepage explaining the concept of the site. If I asked you for help it's probably to add a recipe or two to seed some initial data into the site. Click the Add New Recipe button in the header above. "></NotImpl>
  );
}
